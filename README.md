# chatmvpec2

> Auto-generated by Dev Agent

## Tech Stack

- **Backend**: NestJS + TypeORM + PostgreSQL
- **Frontend**: React + Vite + Tailwind CSS
- **Database**: PostgreSQL 16

## Quick Start

### 1. Start the database

```bash
docker compose up -d
```

### 2. Run the backend

```bash
cd backend
cp .env.example .env    # adjust if needed
npm install
npm run start:dev       # runs on http://localhost:3000
```

API docs: [http://localhost:3000/api/docs](http://localhost:3000/api/docs)

### 3. Run the frontend

```bash
cd frontend
npm install
npm run dev             # runs on http://localhost:5173
```

## Project Structure

```
├── backend/            # NestJS API
│   └── src/

│       ├── auth/

│       ├── chat/

│       ├── app.module.ts
│       └── main.ts
├── frontend/           # React SPA
│   └── src/
│       ├── pages/
│       ├── components/
│       ├── api/
│       └── types/
└── docker-compose.yml
```


## Backend Modules


- **auth**: Register new users with hashed passwords

- **chat**: Create private or group chats with member assignments

